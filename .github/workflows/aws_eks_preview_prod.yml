name: EKS - Preview/Check STAGING 🔍
on:
  pull_request:
    branches:
      - main
    paths:
      - 'aws/eks-gitops/**'
  push:
    branches:
      - main
    paths:
      -  'aws/eks-gitops/**'
  workflow_dispatch:
  # schedule:
  #   - cron: '0 8-17 * * *'

jobs:
  call-preview-workflow:
    uses: ./.github/workflows/aws_eks_preview.yml
    with:
      pulumi-stack: staging # If there would be a separate PROD cluster ... change to "prod"
    secrets: inherit